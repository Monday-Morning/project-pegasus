/// An abstract class that contains all the required constant strings in the
/// project.
abstract class Strings {
  /// The base URL that connects the app with the GraphQL server.
  static const String graphQLURL = 'https://mm.server1.dashnet.in/v1/graph';
}
