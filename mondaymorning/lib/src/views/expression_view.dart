import 'package:flutter/material.dart';

/// Expression View widget of the app.
class ExpressionView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Expression Page'),
    );
  }
}
