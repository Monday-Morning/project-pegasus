import 'package:flutter/material.dart';

/// Connect Page inside the Categories Page.
class ConnectPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Connect Page'),
      ),
    );
  }
}
