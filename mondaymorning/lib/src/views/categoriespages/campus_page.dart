import 'package:flutter/material.dart';

/// Campus Page inside the Categories Page.
class CampusPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Campus Page'),
      ),
    );
  }
}